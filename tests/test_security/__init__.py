"""Security-property tests (ASVS-aligned adversarial / auth / privilege gates).

Marked with ``@pytest.mark.security``. Run via::

    uv run pytest -m security -q --no-cov
"""

from __future__ import annotations
