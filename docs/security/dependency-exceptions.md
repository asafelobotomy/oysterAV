# Dependency audit exceptions

Document `pip-audit --ignore-vuln` IDs here only when a finding is accepted
temporarily. Each entry needs: CVE/PYSEC ID, reason, owner, review-by date.

Currently: **none** (empty allowlist — any pip-audit finding fails CI).
